users(因为病人不需要注册，目前不需要用户身份设计)
- id
- display_name
- role (nurse / doctor)
- email (unique)
patient（可由现有 patients 演进）
- id
- patient_key (unique, not null) // 你现在已有
- doctor_user_id (FK -> users.id)
- name_snapshot, age_snapshot, gender_snapshot
- status (sent / filled / processed)
- requested_at
- filled_at (nullable)
- modified_at (nullable)
case_medications
- id
- patient_id (FK -> patient.id)
- med_name
- dose
- frequency
- start_date / end_date (nullable)
- notes (nullable)
- created_at
case_chat_messages
- id
- patient_id (FK -> patient.id)
- sender_type (patient/bot)
- content_text
- created_at