# dotenvy优先读取容器内的环境变量，这里的环境变量会被dotenvy忽略，程序使用的是docker-compose.yaml中的环境变量
# 这里的环境变量用于本地开发的时候使用
# diesel migration run需要使用这个环境变量
# 记得在容器部署之后运行这个命令：diesel migration run来将数据库迁移到最新版本
DATABASE_URL=postgres://postgres:postgres@localhost:5432/database